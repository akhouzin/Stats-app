{
  "name": "erpgen-stats",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "sync": "node sync-www.js && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
